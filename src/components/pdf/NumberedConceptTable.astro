---
export interface Props {
  items: string[];
}

const { items } = Astro.props;
---

<ol class="numbered-concept-table">
  {
    items.map((item, index) => (
      <li>
        <span>{String(index + 1).padStart(2, "0")}</span>
        <p set:html={item} />
      </li>
    ))
  }
</ol>
